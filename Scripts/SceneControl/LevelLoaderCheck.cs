using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelLoaderCheck : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
         
        Код не рабочий, остатки после отладки. 
        
        print(LevelsData.Levels.Count);
        foreach(var level in LevelsData.Levels)
        {
            print(level.RedFactor + "R " + level.GreenFactor + "G " + level.BlueFactor + "B " + level.OrangeFactor + "O ");

            string order = "";
            foreach(var i in level.GetPlayerBalls())
            {
                order += (BallsColor)i;
            }
            print(order);
        }
        */
    }
}
